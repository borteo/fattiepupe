#main.pure-u
  .content

    = render 'shared/navigation'
    = render 'shared/error'

    %h2= @category.name

    %p#notice= notice

    = link_to 'Edit', edit_category_path(@category)
    \|
    = link_to 'Back', categories_path
