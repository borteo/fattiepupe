#main.pure-u
  .content

    = render 'shared/navigation'
    = render 'shared/error'

    %h2 Listing tags

    %p#notice= notice

    %p
      %b Name:
      = @tag.name

    = link_to 'Edit', edit_tag_path(@tag)
    \|
    = link_to 'Back', tags_path
