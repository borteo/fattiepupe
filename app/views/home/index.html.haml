#main.pure-u    
  
  / %a.addthis_counter{"addthis:description" => "An Example Description", "addthis:title" => "An Example Title", "addthis:url" => "http://example.com", :href => "http://www.addthis.com/bookmark.php"}

  
  .content
    
    .pure-g-r.l-centered
      %h1
        Fatti e Pupe.it
        -if params[:categoria]
          = " - #{params[:categoria]}"
      
    .pure-g-r.l-centered.articles
    
      - @posts.each do |post|
        
        %br
        / Non so come identare la roba qua sotto nel modo giusto!
        .pure-u-1.article
          = link_to image_tag(post.photo.url(:medium), 
          :class => "pure-u addthis_shareable", 
          "addthis:url" => "http://www.fattiepupe.herokuapp.com", 
          "addthis:title" => "fattiepupe.com_cultura"),
          home_path(post)
          
          
          %p.sentence
            = post.title
          %p.sentence
            = post.content

      / .pure-u-1-2
      /   .l-box
      /     %p
      /       - @posts.each do |post|
      /         #{post.name}
      /         #{post.title}
      /         #{post.content}
      /         #{post.image}
      /         #{link_to 'Show', post}
      /         #{link_to 'Edit', edit_post_path(post)}
      /         #{link_to 'Destroy', post, :method => :delete, :data => { :confirm => 'Are you sure?' } }

      /     %p
      /       #{link_to "Post it", posts_path}
